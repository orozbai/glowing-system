/**
 * Contains JPA entity classes representing the domain model of the application.
 *
 * <p>This package includes classes annotated with {@code @Entity} that are
 * mapped to database tables. The entities define the structure of the data,
 * relationships between objects, and persistence configuration used by JPA
 * and Hibernate.</p>
 *
 * <p>Each entity is responsible only for data representation and does not
 * contain business logic.</p>
 */
package com.example.demo.entity;
